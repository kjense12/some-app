import { cp } from 'fs/promises';
import React, { useContext } from 'react';
import { AppContext } from '../state/AppContext';

function Home() {
  return (
      <>
      Home</>
  );
}

export default Home;